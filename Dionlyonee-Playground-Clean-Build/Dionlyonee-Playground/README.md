# Dionlyonee Playground - Clean Build

Upload the CONTENTS of this folder to the ROOT of your GitHub repository.

Before room creation works, paste your Firebase web configuration into:
shared/firebase-config.js

This clean build removes the accidental `javascript` labels, Markdown backticks, mismatched imports, and inconsistent folder paths found in the previous files.
